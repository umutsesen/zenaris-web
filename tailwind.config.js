// tailwind.config.js
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class",
  plugins: [
    require("tailwindcss-animate"),
  ],
  // v4 is CSS-first; theme/content live in CSS via @theme/@config
}
